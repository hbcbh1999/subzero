# Enable schema.sql initialization
spring.datasource.initialization-mode=always
spring.sql.init.mode=always

# Ensure the platform is set if your SQL is database-specific
spring.sql.init.platform=postgres
